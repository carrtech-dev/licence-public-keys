# CarrTech Licence Public Keys

This repository contains public verifier keys for CarrTech licence signing.
It must never contain private signing keys.

CT-Ops release packaging reads `ct-ops/current.pem`. Historic public keys live
under `ct-ops/keys/` and are described by `ct-ops/manifest.json`.
